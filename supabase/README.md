# Supabase Workspace

Place SQL migrations in [`supabase/migrations`](./migrations) and demo seed data in [`supabase/seeds`](./seeds).

The first migration should implement the exact schema and RLS rules from [TRIAGEAI_AGENT_SPEC.md](../TRIAGEAI_AGENT_SPEC.md).
